node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables (contain secrets, never commit)
.env
.env.local
.env.*.local
# Database dumps (contain real data, never commit)
*.sql
*.dump
