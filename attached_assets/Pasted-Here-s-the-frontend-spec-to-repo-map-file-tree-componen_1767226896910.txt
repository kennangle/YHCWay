Here’s the frontend spec-to-repo map (file tree + component contracts + TanStack Query hooks + dnd-kit handlers), mapped to your backend endpoints:
	•	GET /api/projects/:projectId/board → columns + tasksByColumn
	•	PATCH /api/tasks/:taskId/placement → per-project move/reorder
	•	GET /api/tasks/:taskId/stories and POST /api/tasks/:taskId/comments → right pane feed

Assumes React + TypeScript + Vite, React Router, TanStack Query, Zustand, dnd-kit, Tailwind/shadcn. If your repo is already Next.js, this still maps 1:1—just move routes into /app pages.

⸻

1) Repo structure

src/
  app/
    App.tsx
    router.tsx
    layout/
      AppShell.tsx
      Sidebar.tsx
      TopBar.tsx
      ViewTabs.tsx
      GlobalSearch.tsx
  pages/
    HomePage.tsx
    MyTasksPage.tsx
    ProjectPage.tsx
  features/
    projects/
      api.ts
      hooks.ts
      types.ts
      components/
        ProjectHeader.tsx
        ProjectBoardView.tsx
        ProjectListView.tsx
        BoardColumn.tsx
        TaskCard.tsx
        TaskRow.tsx
        InlineTaskComposer.tsx
        ColumnMenu.tsx
    tasks/
      api.ts
      hooks.ts
      types.ts
      dnd/
        placement.ts
        sensors.ts
      components/
        TaskPane.tsx
        TaskPaneHeader.tsx
        TaskDetails.tsx
        TaskProjectsMultiHome.tsx
        DescriptionEditor.tsx
        StoriesFeed.tsx
        CommentComposer.tsx
    stories/
      api.ts
      hooks.ts
      types.ts
  state/
    uiStore.ts
    selectionStore.ts
  lib/
    http.ts
    queryClient.ts
    orderKey.ts
    optimistic.ts
    format.ts
  components/
    ui/   (shadcn)
  styles/
    globals.css


⸻

2) Routing + pane deep-linking contract

src/app/router.tsx

Routes:
	•	/home
	•	/my-tasks
	•	/projects/:projectId?view=list|board&task=:taskId

Rules:
	•	The selected task lives in the URL (task query param).
	•	Closing the pane removes task but preserves view.

Implementation detail:
	•	ProjectPage reads view and task from useSearchParams().
	•	setSearchParams({ task: id }) opens pane; deleting it closes pane.

⸻

3) Shared infra: HTTP client + Query client

src/lib/http.ts

Contract:
	•	Uses cookies/sessions (Passport) → credentials: "include"
	•	Adds tenant header if your backend expects it; otherwise omit.
	•	Normalizes errors.

export type ApiError = { status: number; code: string; message?: string };

async function request<T>(path: string, init?: RequestInit): Promise<T> {
  const res = await fetch(path, {
    ...init,
    headers: { "Content-Type": "application/json", ...(init?.headers || {}) },
    credentials: "include",
  });

  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    throw { status: res.status, code: body?.error || "HTTP_ERROR", message: body?.message } as ApiError;
  }

  return res.json() as Promise<T>;
}

export const http = {
  get: <T>(path: string) => request<T>(path),
  post: <T>(path: string, body?: any) => request<T>(path, { method: "POST", body: JSON.stringify(body ?? {}) }),
  patch: <T>(path: string, body?: any) => request<T>(path, { method: "PATCH", body: JSON.stringify(body ?? {}) }),
};

src/lib/queryClient.ts
	•	Standard QueryClient with retries off for mutations, modest stale times.

⸻

4) Domain types mapped to your endpoints

src/features/projects/types.ts

export type ProjectColumn = {
  id: number;
  projectId: number;
  name: string;
  // you may also have sortOrder/orderKey; keep what exists:
  sortOrder?: number;
};

export type Placement = {
  projectId: number;
  columnId: number | null;
  sortOrder: number;
  orderKey: string | null;
};

export type TaskLite = {
  id: number;
  name: string;
  completed?: boolean;
  assigneeId?: string | null;
  dueOn?: string | null;
  // any other lightweight fields you already return in board payload
  placement: Placement;
};

export type ProjectBoardResponse = {
  columns: ProjectColumn[];
  tasksByColumn: Record<string, TaskLite[]>; // key: columnId or "null"
};

src/features/tasks/types.ts

export type Task = {
  id: number;
  name: string;
  completed: boolean;
  assigneeId: string | null;
  dueOn: string | null;
  description?: string | null;
  // optionally include placements list if you expose it later:
  // placements?: Placement[];
};

src/features/stories/types.ts

export type Story = {
  id: number;
  storyType: "comment" | "activity";
  authorId: string | null;
  createdAt: string;
  body: string | null;
  activityType: string | null;
  activityPayload: any;
  isEdited: boolean;
  editedAt: string | null;
};

export type StoriesResponse = { stories: Story[] };


⸻

5) API modules (thin, predictable)

src/features/projects/api.ts

import { http } from "@/lib/http";
import type { ProjectBoardResponse } from "./types";

export const projectsApi = {
  board: (projectId: number) =>
    http.get<ProjectBoardResponse>(`/api/projects/${projectId}/board`),
};

src/features/tasks/api.ts

import { http } from "@/lib/http";

export const tasksApi = {
  updatePlacement: (taskId: number, body: {
    projectId: number;
    columnId: number | null;
    sortOrder?: number;
    orderKey?: string | null;
  }) => http.patch<{ ok: true }>(`/api/tasks/${taskId}/placement`, body),

  // If you already have task detail endpoint, wire it here.
  // get: (taskId: number) => http.get<Task>(`/api/tasks/${taskId}`),
};

src/features/stories/api.ts

import { http } from "@/lib/http";
import type { StoriesResponse } from "./types";

export const storiesApi = {
  list: (taskId: number) => http.get<StoriesResponse>(`/api/tasks/${taskId}/stories`),
  comment: (taskId: number, body: string) =>
    http.post<{ id: number }>(`/api/tasks/${taskId}/comments`, { body }),
};


⸻

6) Query hooks (TanStack Query) + cache shapes

src/features/projects/hooks.ts

import { useQuery, useQueryClient } from "@tanstack/react-query";
import { projectsApi } from "./api";

export const projectKeys = {
  board: (projectId: number) => ["projectBoard", projectId] as const,
};

export function useProjectBoard(projectId: number) {
  return useQuery({
    queryKey: projectKeys.board(projectId),
    queryFn: () => projectsApi.board(projectId),
    staleTime: 10_000,
  });
}

export function useInvalidateProjectBoard() {
  const qc = useQueryClient();
  return (projectId: number) => qc.invalidateQueries({ queryKey: projectKeys.board(projectId) });
}

src/features/stories/hooks.ts

import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { storiesApi } from "./api";

export const storyKeys = {
  list: (taskId: number) => ["taskStories", taskId] as const,
};

export function useTaskStories(taskId: number | null) {
  return useQuery({
    queryKey: taskId ? storyKeys.list(taskId) : ["taskStories", "none"],
    queryFn: () => storiesApi.list(taskId!),
    enabled: !!taskId,
    staleTime: 5_000,
  });
}

export function useCreateComment(taskId: number) {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (body: string) => storiesApi.comment(taskId, body),
    onMutate: async (body) => {
      await qc.cancelQueries({ queryKey: storyKeys.list(taskId) });
      const prev = qc.getQueryData<any>(storyKeys.list(taskId));

      const optimistic = {
        id: -Date.now(),
        storyType: "comment",
        authorId: "me",
        createdAt: new Date().toISOString(),
        body,
        activityType: null,
        activityPayload: null,
        isEdited: false,
        editedAt: null,
        _optimistic: true,
      };

      qc.setQueryData(storyKeys.list(taskId), (old: any) => ({
        stories: [...(old?.stories ?? []), optimistic],
      }));

      return { prev };
    },
    onError: (_err, _body, ctx) => {
      if (ctx?.prev) qc.setQueryData(storyKeys.list(taskId), ctx.prev);
    },
    onSuccess: () => {
      // refetch to get server IDs + any outbox activity that landed quickly
      qc.invalidateQueries({ queryKey: storyKeys.list(taskId) });
    },
  });
}


⸻

7) UI state stores (Zustand) for “Asana-ish” feel

src/state/uiStore.ts

import { create } from "zustand";

type UIState = {
  sidebarCollapsed: boolean;
  setSidebarCollapsed: (v: boolean) => void;
};

export const useUIStore = create<UIState>((set) => ({
  sidebarCollapsed: false,
  setSidebarCollapsed: (v) => set({ sidebarCollapsed: v }),
}));

Selection should mostly come from URL, but you can mirror it for convenience:

src/state/selectionStore.ts

import { create } from "zustand";

type SelectionState = {
  selectedTaskId: number | null;
  setSelectedTaskId: (id: number | null) => void;
};

export const useSelectionStore = create<SelectionState>((set) => ({
  selectedTaskId: null,
  setSelectedTaskId: (id) => set({ selectedTaskId: id }),
}));

Contract:
	•	URL is source of truth.
	•	Store mirrors for quick access / UI highlight when needed.

⸻

8) ProjectPage composition and view switching

src/pages/ProjectPage.tsx

Responsibilities:
	•	Read projectId
	•	Read view + task from query string
	•	Render ProjectHeader + ViewTabs
	•	Render ProjectBoardView or ProjectListView
	•	Render TaskPane if task selected

Acceptance criteria:
	•	Switching view updates ?view= without losing selected task.
	•	Clicking a task sets ?task= and opens pane.

⸻

9) Project Board View spec (dnd-kit + optimistic placement)

Component contracts

ProjectBoardView
Props:
	•	projectId: number
	•	columns: ProjectColumn[]
	•	tasksByColumn: Record<string, TaskLite[]>
	•	onSelectTask(taskId: number): void

Responsibilities:
	•	Render columns horizontally
	•	Provide DnD context
	•	On drop: compute new orderKey (or sortOrder) and call mutation

BoardColumn
Props:
	•	column: ProjectColumn
	•	tasks: TaskLite[]
	•	projectId: number
	•	onSelectTask(id: number): void

TaskCard
Props:
	•	task: TaskLite
	•	onClick(): void

DnD wiring files

src/features/tasks/dnd/sensors.ts
	•	Use pointer sensor + keyboard sensor
	•	Activation distance to avoid accidental drags

src/features/tasks/dnd/placement.ts
Core logic: given a drag result, compute:
	•	toColumnId
	•	new ordering between neighbors: orderKey = between(prev.orderKey, next.orderKey)
If you haven’t fully rolled out orderKey, fallback to sortOrder.

src/lib/orderKey.ts (frontend counterpart)

You already have a backend between() strategy; mirror it in the client so drop feels instant.

Contract:
	•	between(prev: string | null, next: string | null): string
	•	deterministic and stable

(Use the same minimal midpoint scheme you used server-side, or a LexoRank lib later.)

⸻

10) Placement mutation hook with optimistic update

src/features/tasks/hooks.ts

import { useMutation, useQueryClient } from "@tanstack/react-query";
import { tasksApi } from "./api";
import { projectKeys } from "@/features/projects/hooks";

export function useMovePlacement(projectId: number) {
  const qc = useQueryClient();

  return useMutation({
    mutationFn: (input: {
      taskId: number;
      projectId: number;
      columnId: number | null;
      orderKey?: string | null;
      sortOrder?: number;
    }) => tasksApi.updatePlacement(input.taskId, {
      projectId: input.projectId,
      columnId: input.columnId,
      orderKey: input.orderKey ?? null,
      sortOrder: input.sortOrder,
    }),

    onMutate: async (input) => {
      await qc.cancelQueries({ queryKey: projectKeys.board(projectId) });
      const prev = qc.getQueryData<any>(projectKeys.board(projectId));

      // optimistic patch inside tasksByColumn
      qc.setQueryData(projectKeys.board(projectId), (old: any) => {
        if (!old) return old;

        // Remove from any column list it currently resides in
        const next = structuredClone(old);
        const fromKey = Object.keys(next.tasksByColumn).find((k) =>
          next.tasksByColumn[k]?.some((t: any) => t.id === input.taskId)
        );

        let task: any = null;
        if (fromKey) {
          const idx = next.tasksByColumn[fromKey].findIndex((t: any) => t.id === input.taskId);
          task = next.tasksByColumn[fromKey][idx];
          next.tasksByColumn[fromKey].splice(idx, 1);
        }

        if (!task) return next;

        // Insert into destination column list (you can insert by computed index; simplest append then sort)
        const toKey = input.columnId == null ? "null" : String(input.columnId);
        if (!next.tasksByColumn[toKey]) next.tasksByColumn[toKey] = [];

        task.placement = {
          ...task.placement,
          projectId: input.projectId,
          columnId: input.columnId,
          orderKey: input.orderKey ?? task.placement.orderKey,
          sortOrder: input.sortOrder ?? task.placement.sortOrder,
        };

        next.tasksByColumn[toKey].push(task);

        // Optional: sort by orderKey/sortOrder to keep stable
        next.tasksByColumn[toKey].sort((a: any, b: any) => {
          const ak = a.placement.orderKey ?? "";
          const bk = b.placement.orderKey ?? "";
          if (ak && bk) return ak.localeCompare(bk);
          return (a.placement.sortOrder ?? 0) - (b.placement.sortOrder ?? 0);
        });

        return next;
      });

      return { prev };
    },

    onError: (_err, _input, ctx) => {
      if (ctx?.prev) qc.setQueryData(projectKeys.board(projectId), ctx.prev);
    },

    onSuccess: () => {
      qc.invalidateQueries({ queryKey: projectKeys.board(projectId) });
    },
  });
}

Acceptance criteria:
	•	Cross-column drop is instant (optimistic).
	•	Failed move reverts and shows an error toast.
	•	Refresh shows identical order (server is source of truth).

⸻

11) Project List View spec (same data, different rendering)

ProjectListView contract

Props:
	•	same as board, but rendered as vertical grouped sections
	•	DnD works for row reorder + cross-section move
	•	Inline task create per section

Key rule:
	•	List view uses the same placement mutation as board view.

⸻

12) Task Pane spec-to-repo map (URL-driven, query-driven)

TaskPane behavior

Inputs:
	•	taskId: number
	•	onClose(): void

Data:
	•	useTaskStories(taskId) (wired)
	•	(Optional) useTask(taskId) if you add GET /api/tasks/:id

Render:
	•	Header (complete toggle + title)
	•	Details (assignee, due date)
	•	Description
	•	StoriesFeed + CommentComposer

Stories modules
	•	StoriesFeed uses useTaskStories(taskId) and renders:
	•	comment entries
	•	activity entries (arrive later from outbox)
	•	CommentComposer uses useCreateComment(taskId) with optimistic insert

Outbox lag strategy:
	•	When pane is open:
	•	refetch stories on window focus
	•	optional: poll every 15–30s (only while pane open)

Acceptance criteria:
	•	Comment appears immediately and then stabilizes with server id.
	•	Activity shows up without breaking the feed if delayed.

⸻

13) Wiring the “Asana-ish” polish points

Keyboard shortcuts (minimal)

Files:
	•	src/app/layout/Shortcuts.tsx
Rules:
	•	Esc closes pane if open (priority)
	•	Tab+Q opens quick add modal (optional)
	•	Disable shortcuts when active element is an input/editor

Global search
	•	GlobalSearch.tsx is a shell in MVP
	•	If you don’t have /api/search yet, show recent items (from cached projects/board) and stub the query.

Loading states
	•	Skeletons for board/list and task pane
	•	Keep shell visible (never blank the whole page on fetch)

⸻

14) “Definition of done” for the frontend MVP feel

You’ll know you’ve hit “approaches Asana UI” when:
	•	Sidebar + project view tabs exist and feel stable.
	•	Clicking tasks opens a right pane without losing context, and the URL deep-links.
	•	Board drag/drop updates placement instantly and persists.
	•	List view supports inline edits and add-task-in-section flow.
	•	Stories feed shows comments immediately; activity arrives later via outbox without confusion.

⸻

15) Implementation order (fastest path to the vibe)
	1.	AppShell + Router + /projects/:id?view=&task=
	2.	useProjectBoard + ProjectBoardView rendering
	3.	Task selection + TaskPane with Stories (GET + POST comments)
	4.	dnd-kit placement update + optimistic cache patch
	5.	ProjectListView using the same data + same placement mutation
	6.	Keyboard polish + skeletons + basic search stub

⸻

If you paste your exact GET /api/projects/:id/board JSON (one real example), I’ll lock the types.ts and the cache patch logic to your actual field names so you can copy/paste without a TypeScript wrestling match.