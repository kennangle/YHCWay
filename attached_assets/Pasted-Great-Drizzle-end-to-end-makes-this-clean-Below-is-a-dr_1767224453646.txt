Great — Drizzle end-to-end makes this clean. Below is a drop-in “starter PR” kit that matches your current realities:
	•	Projects/Tasks IDs: serial int
	•	Users: varchar (sub from Replit OIDC)
	•	Tenant: varchar on rows, sourced from req.tenantId
	•	Storage: Drizzle query builder everywhere
	•	Goal: upgrade your existing single-homed system into a real Asana facsimile with multi-homing + placement + stories + outbox worker — without blowing up your current interface.

I’m giving you code in four layers:
	1.	Schema additions (task_projects, task_stories, event_outbox)
	2.	Storage methods (new methods + compatibility wrappers)
	3.	Express routes (placement + stories + board read)
	4.	Outbox worker (claims events + writes activity stories)

Everything is explicit and implementation-oriented.

⸻

1) Schema additions (Drizzle)

These are additive. Keep your existing projects, tasks, projectColumns (or whatever you call them), etc.

src/db/schema/taskProjects.ts

import {
  pgTable,
  serial,
  integer,
  varchar,
  text,
  timestamp,
  primaryKey,
  index,
} from "drizzle-orm/pg-core";
import { projects } from "./projects";
import { tasks } from "./tasks";
import { projectColumns } from "./projectColumns"; // rename to match your schema file

/**
 * task_projects = Asana multi-homing + per-project placement.
 *
 * Transitional compatibility:
 * - Keep sortOrder for current UI/logic (int)
 * - Add orderKey for stable fractional ordering (text)
 *
 * Rule:
 * - If orderKey is present, order by orderKey.
 * - Else order by sortOrder.
 */
export const taskProjects = pgTable(
  "task_projects",
  {
    tenantId: varchar("tenant_id", { length: 64 }).notNull(),

    taskId: integer("task_id")
      .notNull()
      .references(() => tasks.id, { onDelete: "cascade" }),

    projectId: integer("project_id")
      .notNull()
      .references(() => projects.id, { onDelete: "cascade" }),

    columnId: integer("column_id").references(() => projectColumns.id, {
      onDelete: "set null",
    }),

    // Compatibility for existing moveTask(taskId, columnId, sortOrder)
    sortOrder: integer("sort_order").notNull().default(0),

    // New recommended ordering key (lexicographic). Start writing this now.
    orderKey: text("order_key"),

    addedBy: varchar("added_by", { length: 128 }).notNull(), // userId (Replit sub)
    addedAt: timestamp("added_at", { withTimezone: true })
      .notNull()
      .defaultNow(),
  },
  (t) => ({
    pk: primaryKey({ columns: [t.taskId, t.projectId] }),

    byProject: index("task_projects_by_project").on(
      t.tenantId,
      t.projectId,
      t.columnId,
      t.sortOrder
    ),

    byTask: index("task_projects_by_task").on(t.tenantId, t.taskId),
  })
);

src/db/schema/taskStories.ts

import {
  pgTable,
  serial,
  integer,
  varchar,
  text,
  timestamp,
  boolean,
  jsonb,
  index,
} from "drizzle-orm/pg-core";
import { tasks } from "./tasks";

/**
 * task_stories = unified stream: comments + immutable activity.
 *
 * storyType:
 * - "comment": body is required; activity fields null
 * - "activity": activityType + activityPayload required; body null
 */
export const taskStories = pgTable(
  "task_stories",
  {
    id: serial("id").primaryKey(),
    tenantId: varchar("tenant_id", { length: 64 }).notNull(),

    taskId: integer("task_id")
      .notNull()
      .references(() => tasks.id, { onDelete: "cascade" }),

    storyType: text("story_type").notNull(), // comment|activity

    // null means "system actor"
    authorId: varchar("author_id", { length: 128 }),

    createdAt: timestamp("created_at", { withTimezone: true })
      .notNull()
      .defaultNow(),

    // Comments
    body: text("body"),

    // Activity
    activityType: text("activity_type"),
    activityPayload: jsonb("activity_payload"),

    // Comment edit metadata
    isEdited: boolean("is_edited").notNull().default(false),
    editedAt: timestamp("edited_at", { withTimezone: true }),
  },
  (t) => ({
    byTask: index("task_stories_by_task").on(t.tenantId, t.taskId, t.createdAt),
  })
);

src/db/schema/eventOutbox.ts

import {
  pgTable,
  bigserial,
  varchar,
  text,
  timestamp,
  jsonb,
  index,
} from "drizzle-orm/pg-core";

/**
 * event_outbox = transactional outbox for consistent side effects.
 * Worker claims rows using SKIP LOCKED and marks publishedAt.
 */
export const eventOutbox = pgTable(
  "event_outbox",
  {
    id: bigserial("id", { mode: "number" }).primaryKey(),
    tenantId: varchar("tenant_id", { length: 64 }).notNull(),

    eventType: text("event_type").notNull(), // task.created|task.updated|task.moved|story.created...
    entityType: text("entity_type").notNull(), // task|story|project
    entityId: text("entity_id").notNull(), // "task:123" | "story:999" (string so it can hold anything)
    payload: jsonb("payload").notNull(),

    createdAt: timestamp("created_at", { withTimezone: true })
      .notNull()
      .defaultNow(),

    publishedAt: timestamp("published_at", { withTimezone: true }),
  },
  (t) => ({
    pending: index("event_outbox_pending").on(t.publishedAt, t.createdAt),
    byTenantType: index("event_outbox_tenant_type").on(t.tenantId, t.eventType),
  })
);


⸻

2) Storage: new methods + compatibility wrappers (Drizzle)

This assumes you have a storage class implementing IStorage and a db Drizzle instance.

2.1 Add these new methods to IStorage

Additive (doesn’t break existing callers):

// Multi-homing + placement
addTaskToProject(params: {
  tenantId: string;
  taskId: number;
  projectId: number;
  columnId?: number | null;
  sortOrder?: number;
  orderKey?: string | null;
  addedBy: string;
}): Promise<void>;

removeTaskFromProject(params: {
  tenantId: string;
  taskId: number;
  projectId: number;
}): Promise<void>;

updateTaskPlacement(params: {
  tenantId: string;
  taskId: number;
  projectId: number;
  columnId: number | null;
  sortOrder?: number;
  orderKey?: string | null;
  movedBy: string;
}): Promise<void>;

getProjectTaskPlacements(projectId: number, tenantId: string): Promise<Array<{
  task: Task;
  projectId: number;
  columnId: number | null;
  sortOrder: number;
  orderKey: string | null;
}>>;

// Stories
getTaskStories(taskId: number, tenantId: string): Promise<Array<{
  id: number;
  storyType: "comment" | "activity";
  authorId: string | null;
  createdAt: Date;
  body: string | null;
  activityType: string | null;
  activityPayload: any;
  isEdited: boolean;
  editedAt: Date | null;
}>>;

createTaskStoryComment(params: {
  tenantId: string;
  taskId: number;
  authorId: string;
  body: string;
}): Promise<{ id: number }>;

createTaskStoryActivity(params: {
  tenantId: string;
  taskId: number;
  activityType: string;
  activityPayload: any;
  actorId?: string | null;
}): Promise<{ id: number }>;

// Outbox
enqueueOutboxEvent(params: {
  tenantId: string;
  eventType: string;
  entityType: string;
  entityId: string;
  payload: any;
}): Promise<void>;

claimNextOutboxEvent(tenantId?: string): Promise<{
  id: number;
  tenantId: string;
  eventType: string;
  entityType: string;
  entityId: string;
  payload: any;
} | null>;

markOutboxPublished(id: number): Promise<void>;

2.2 Implementations (Drizzle syntax)

src/storage/drizzleStorage.asana.ts (mixin-style additions)

import { and, eq, sql, asc } from "drizzle-orm";
import { db } from "@/db";
import { taskProjects } from "@/db/schema/taskProjects";
import { taskStories } from "@/db/schema/taskStories";
import { eventOutbox } from "@/db/schema/eventOutbox";
import { tasks } from "@/db/schema/tasks";

// Optional helper: maintain stable lexical ordering if you only have sortOrder today.
function orderKeyFromSortOrder(n: number) {
  // 0000000123 sorts correctly as text
  return String(n).padStart(10, "0");
}

export class DrizzleStorageAsana {
  // ----- MULTI-HOMING / PLACEMENT -----

  async addTaskToProject(params: {
    tenantId: string;
    taskId: number;
    projectId: number;
    columnId?: number | null;
    sortOrder?: number;
    orderKey?: string | null;
    addedBy: string;
  }): Promise<void> {
    const sortOrder = params.sortOrder ?? 0;
    const orderKey = params.orderKey ?? orderKeyFromSortOrder(sortOrder);

    await db
      .insert(taskProjects)
      .values({
        tenantId: params.tenantId,
        taskId: params.taskId,
        projectId: params.projectId,
        columnId: params.columnId ?? null,
        sortOrder,
        orderKey,
        addedBy: params.addedBy,
      })
      .onConflictDoUpdate({
        target: [taskProjects.taskId, taskProjects.projectId],
        set: {
          columnId: params.columnId ?? null,
          sortOrder,
          orderKey,
        },
      });
  }

  async removeTaskFromProject(params: {
    tenantId: string;
    taskId: number;
    projectId: number;
  }): Promise<void> {
    await db
      .delete(taskProjects)
      .where(
        and(
          eq(taskProjects.tenantId, params.tenantId),
          eq(taskProjects.taskId, params.taskId),
          eq(taskProjects.projectId, params.projectId)
        )
      );
  }

  async updateTaskPlacement(params: {
    tenantId: string;
    taskId: number;
    projectId: number;
    columnId: number | null;
    sortOrder?: number;
    orderKey?: string | null;
    movedBy: string;
  }): Promise<void> {
    const sortOrder = params.sortOrder ?? 0;
    const orderKey = params.orderKey ?? orderKeyFromSortOrder(sortOrder);

    const updated = await db
      .update(taskProjects)
      .set({
        columnId: params.columnId,
        sortOrder,
        orderKey,
      })
      .where(
        and(
          eq(taskProjects.tenantId, params.tenantId),
          eq(taskProjects.taskId, params.taskId),
          eq(taskProjects.projectId, params.projectId)
        )
      )
      .returning({ taskId: taskProjects.taskId });

    if (updated.length === 0) {
      throw new Error("TASK_NOT_IN_PROJECT");
    }

    // Emit outbox event for worker to create activity story
    await this.enqueueOutboxEvent({
      tenantId: params.tenantId,
      eventType: "task.moved",
      entityType: "task",
      entityId: `task:${params.taskId}`,
      payload: {
        taskId: params.taskId,
        projectId: params.projectId,
        columnId: params.columnId,
        sortOrder,
        orderKey,
        movedBy: params.movedBy,
      },
    });
  }

  async getProjectTaskPlacements(projectId: number, tenantId: string): Promise<Array<{
    task: any;
    projectId: number;
    columnId: number | null;
    sortOrder: number;
    orderKey: string | null;
  }>> {
    const rows = await db
      .select({
        task: tasks,
        projectId: taskProjects.projectId,
        columnId: taskProjects.columnId,
        sortOrder: taskProjects.sortOrder,
        orderKey: taskProjects.orderKey,
      })
      .from(taskProjects)
      .innerJoin(tasks, eq(tasks.id, taskProjects.taskId))
      .where(and(eq(taskProjects.tenantId, tenantId), eq(taskProjects.projectId, projectId)))
      // Prefer orderKey when present, otherwise sortOrder; both are stored so we can just sort by both
      .orderBy(asc(taskProjects.columnId), asc(taskProjects.orderKey), asc(taskProjects.sortOrder));

    return rows;
  }

  // ----- STORIES -----

  async getTaskStories(taskId: number, tenantId: string) {
    return db
      .select({
        id: taskStories.id,
        storyType: taskStories.storyType,
        authorId: taskStories.authorId,
        createdAt: taskStories.createdAt,
        body: taskStories.body,
        activityType: taskStories.activityType,
        activityPayload: taskStories.activityPayload,
        isEdited: taskStories.isEdited,
        editedAt: taskStories.editedAt,
      })
      .from(taskStories)
      .where(and(eq(taskStories.tenantId, tenantId), eq(taskStories.taskId, taskId)))
      .orderBy(asc(taskStories.createdAt));
  }

  async createTaskStoryComment(params: {
    tenantId: string;
    taskId: number;
    authorId: string;
    body: string;
  }) {
    const [row] = await db
      .insert(taskStories)
      .values({
        tenantId: params.tenantId,
        taskId: params.taskId,
        storyType: "comment",
        authorId: params.authorId,
        body: params.body,
      })
      .returning({ id: taskStories.id });

    await this.enqueueOutboxEvent({
      tenantId: params.tenantId,
      eventType: "story.created",
      entityType: "story",
      entityId: `story:${row.id}`,
      payload: { storyId: row.id, taskId: params.taskId, authorId: params.authorId },
    });

    return row;
  }

  async createTaskStoryActivity(params: {
    tenantId: string;
    taskId: number;
    activityType: string;
    activityPayload: any;
    actorId?: string | null;
  }) {
    const [row] = await db
      .insert(taskStories)
      .values({
        tenantId: params.tenantId,
        taskId: params.taskId,
        storyType: "activity",
        authorId: params.actorId ?? null,
        activityType: params.activityType,
        activityPayload: params.activityPayload,
      })
      .returning({ id: taskStories.id });

    return row;
  }

  // ----- OUTBOX -----

  async enqueueOutboxEvent(params: {
    tenantId: string;
    eventType: string;
    entityType: string;
    entityId: string;
    payload: any;
  }) {
    await db.insert(eventOutbox).values({
      tenantId: params.tenantId,
      eventType: params.eventType,
      entityType: params.entityType,
      entityId: params.entityId,
      payload: params.payload,
    });
  }

  /**
   * Claim the next unprocessed outbox event using SKIP LOCKED.
   * This is implemented using db.execute(sql`...`) because Drizzle doesn't
   * expose SKIP LOCKED via the query builder in a portable way.
   */
  async claimNextOutboxEvent(tenantId?: string) {
    const whereTenant = tenantId
      ? sql`AND tenant_id = ${tenantId}`
      : sql``;

    const result = await db.execute(sql`
      WITH next_evt AS (
        SELECT id
        FROM event_outbox
        WHERE published_at IS NULL
        ${whereTenant}
        ORDER BY created_at ASC
        FOR UPDATE SKIP LOCKED
        LIMIT 1
      )
      SELECT eo.*
      FROM event_outbox eo
      JOIN next_evt ne ON ne.id = eo.id
    `);

    // @ts-ignore Drizzle returns driver-specific shape; normalize defensively
    const rows = result.rows ?? result;

    if (!rows || rows.length === 0) return null;

    const evt = rows[0];
    return {
      id: Number(evt.id),
      tenantId: String(evt.tenant_id),
      eventType: String(evt.event_type),
      entityType: String(evt.entity_type),
      entityId: String(evt.entity_id),
      payload: evt.payload,
    };
  }

  async markOutboxPublished(id: number) {
    await db.update(eventOutbox).set({ publishedAt: new Date() }).where(eq(eventOutbox.id, id));
  }
}

Compatibility wrapper: make your existing moveTask stop lying

Your existing moveTask(taskId, columnId, sortOrder) currently “moves a task” globally. To avoid that, during migration:
	•	treat it as moving the task in its current project only
	•	update task_projects
	•	emit outbox event → activity story

You’ll need task’s current projectId and tenantId (likely present on tasks row today). Here’s a safe pattern:

import { eq } from "drizzle-orm";
import { tasks } from "@/db/schema/tasks";

async moveTask(taskId: number, columnId: number, sortOrder: number) {
  const task = await db.select().from(tasks).where(eq(tasks.id, taskId)).then(r => r[0]);
  if (!task) return undefined;

  // Assuming your tasks table currently includes tenantId + projectId
  const tenantId = (task as any).tenantId;
  const projectId = (task as any).projectId;

  // Update old columns for backwards compatibility
  const [updated] = await db.update(tasks).set({
    ...(tasks as any).columnId ? { columnId } : {},
    ...(tasks as any).sortOrder ? { sortOrder } : {},
  }).where(eq(tasks.id, taskId)).returning();

  // Update placement (real source of truth going forward)
  await this.updateTaskPlacement({
    tenantId,
    taskId,
    projectId,
    columnId,
    sortOrder,
    movedBy: "SYSTEM_COMPAT", // pass req.user.sub in real call path
  });

  return updated;
}

You’ll want to route UI calls to updateTaskPlacement directly soon. This wrapper is just to keep the ship afloat during migration.

⸻

3) Express routes that unlock the Asana feel immediately

These assume:
	•	req.user.claims.sub is userId
	•	req.tenantId is set
	•	you already have ensureAuth and tenant middleware

3.1 Placement route (real per-project movement)

PATCH /api/tasks/:taskId/placement

import { Router } from "express";
import { ensureAuth } from "../auth/ensureAuth";
import { getUserId } from "../auth/types";

export function tasksPlacementRoutes(storage: any) {
  const r = Router();

  r.patch("/tasks/:taskId/placement", ensureAuth, async (req, res) => {
    const userId = getUserId(req as any);
    const tenantId = (req as any).tenantId as string;
    const taskId = Number(req.params.taskId);

    const { projectId, columnId, sortOrder, orderKey } = req.body ?? {};
    if (!projectId) return res.status(400).json({ error: "PROJECT_ID_REQUIRED" });

    try {
      await storage.updateTaskPlacement({
        tenantId,
        taskId,
        projectId: Number(projectId),
        columnId: columnId === null || columnId === undefined ? null : Number(columnId),
        sortOrder: sortOrder === undefined ? undefined : Number(sortOrder),
        orderKey: orderKey ?? null,
        movedBy: userId!,
      });

      return res.json({ ok: true });
    } catch (e: any) {
      if (String(e?.message) === "TASK_NOT_IN_PROJECT") {
        return res.status(404).json({ error: "TASK_NOT_IN_PROJECT" });
      }
      return res.status(500).json({ error: "INTERNAL_ERROR" });
    }
  });

  return r;
}

3.2 Stories routes (comments + activity feed)
	•	GET /api/tasks/:taskId/stories
	•	POST /api/tasks/:taskId/comments

import { Router } from "express";
import { ensureAuth } from "../auth/ensureAuth";
import { getUserId } from "../auth/types";

export function taskStoriesRoutes(storage: any) {
  const r = Router();

  r.get("/tasks/:taskId/stories", ensureAuth, async (req, res) => {
    const tenantId = (req as any).tenantId as string;
    const taskId = Number(req.params.taskId);

    const stories = await storage.getTaskStories(taskId, tenantId);
    res.json({ stories });
  });

  r.post("/tasks/:taskId/comments", ensureAuth, async (req, res) => {
    const tenantId = (req as any).tenantId as string;
    const userId = getUserId(req as any)!;
    const taskId = Number(req.params.taskId);
    const { body } = req.body ?? {};
    if (!body) return res.status(400).json({ error: "BODY_REQUIRED" });

    const created = await storage.createTaskStoryComment({
      tenantId,
      taskId,
      authorId: userId,
      body: String(body),
    });

    res.status(201).json(created);
  });

  return r;
}

3.3 Project board read (placement-aware)

GET /api/projects/:projectId/board

This is the endpoint that makes your board feel like Asana because it returns columns + tasks grouped by placement.

import { Router } from "express";
import { ensureAuth } from "../auth/ensureAuth";

export function projectBoardRoutes(storage: any) {
  const r = Router();

  r.get("/projects/:projectId/board", ensureAuth, async (req, res) => {
    const tenantId = (req as any).tenantId as string;
    const projectId = Number(req.params.projectId);

    const columns = await storage.getProjectColumns(projectId);
    const placements = await storage.getProjectTaskPlacements(projectId, tenantId);

    // Group tasks by columnId
    const byColumn: Record<string, any[]> = {};
    for (const row of placements) {
      const key = row.columnId == null ? "null" : String(row.columnId);
      if (!byColumn[key]) byColumn[key] = [];
      byColumn[key].push({
        ...row.task,
        placement: {
          projectId: row.projectId,
          columnId: row.columnId,
          sortOrder: row.sortOrder,
          orderKey: row.orderKey,
        },
      });
    }

    res.json({
      columns,
      tasksByColumn: byColumn,
    });
  });

  return r;
}


⸻

4) Outbox worker (Drizzle) that creates immutable activity stories

This is the “adult supervision” that keeps side effects consistent.

src/workers/outboxWorker.ts

export async function runOutboxWorker(storage: any, opts?: { tenantId?: string; idleMs?: number }) {
  const idleMs = opts?.idleMs ?? 300;

  // eslint-disable-next-line no-constant-condition
  while (true) {
    // claim event
    const evt = await storage.claimNextOutboxEvent(opts?.tenantId);

    if (!evt) {
      await new Promise((r) => setTimeout(r, idleMs));
      continue;
    }

    try {
      // mark published early to avoid duplicate processing on crash
      await storage.markOutboxPublished(evt.id);

      // dispatch
      await handleEvent(storage, evt);
    } catch (e) {
      // You can add a dead-letter mechanism later. For MVP, log.
      console.error("Outbox processing error:", e, evt);
    }
  }
}

async function handleEvent(storage: any, evt: any) {
  const p = evt.payload;

  switch (evt.eventType) {
    case "task.created":
      return storage.createTaskStoryActivity({
        tenantId: evt.tenantId,
        taskId: p.taskId,
        activityType: "task.created",
        activityPayload: p,
        actorId: p.createdBy ?? null,
      });

    case "task.moved":
      return storage.createTaskStoryActivity({
        tenantId: evt.tenantId,
        taskId: p.taskId,
        activityType: "task.moved",
        activityPayload: p,
        actorId: p.movedBy ?? null,
      });

    case "story.created":
      // Later: parse mentions → notifications. Keep simple for MVP.
      return;

    default:
      return;
  }
}


⸻

5) Immediate “Asana facsimile” impact (what you’ll see once this is wired)

After you add these pieces and switch your board reads to task_projects:
	•	Moving a task is per project (correct semantics)
	•	Your board/list ordering is stable (no renumber churn once you start using orderKey)
	•	Task details show comments + activity in a single feed
	•	Side effects (activity feed entries) are consistent because they’re worker-driven

That’s the moment you stop being todo app #9000.

⸻

6) What I recommend you do next in your repo (no extra decisions needed)
	1.	Add the 3 schema files and run migrations
	2.	Implement the storage class additions exactly as above
	3.	Wire these routes:
	•	PATCH /tasks/:taskId/placement
	•	GET /projects/:projectId/board
	•	GET/POST /tasks/:taskId/stories|comments
	4.	Start the outbox worker in a separate process in Replit (or as a separate “Run” command)

If you paste your actual Drizzle schemas for tasks, projects, and projectColumns (just those three table definitions), I’ll adjust the imports/field names so these files compile first try—no guessing about column names like projectColumns.id vs project_columns.id.